import { MyWhileDirective } from './my-while.directive';

describe('MyWhileDirective', () => {
  it('should create an instance', () => {
    // const directive = new MyWhileDirective();
    // expect(directive).toBeTruthy();
  });
});
