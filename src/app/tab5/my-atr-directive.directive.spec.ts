import { MyAtrDirectiveDirective } from './my-atr-directive.directive';

describe('MyAtrDirectiveDirective', () => {
  it('should create an instance', () => {
    //  const directive = new MyAtrDirectiveDirective();
    // expect(directive).toBeTruthy();
  });
});
